<div class="row">

    <div id="content" class="col-md-9 col-sm-12 mx-auto">
        <h3>Exámenes del horario</h3>

        <h4>Convocatoria ordinaria</h4>
        <% if (examenes) { %>
        <table class="table table-striped">
            <thead class="thead-light">
                <tr>
                    <th scope="col">Fecha</th>
                    <th scope="col">Hora</th>
                    <th scope="col">Asignatura</th>
                    <th scope="col">Duración</th>
                </tr>
            </thead>
            <tbody>
                <% examenes.ordinaria.forEach((exam)=>{ %>
                <tr>
                    <td><%= exam.fecha ? exam.fecha : '-' %></td>
                    <td><%= exam.hora ? exam.hora : '-' %></td>
                    <td><%= exam.asignatura %></td>
                    <td><%= exam.duracion ? exam.duracion : '-' %></td>
                </tr>
                <% }); %>
            </tbody>
        </table>
        <% } %>

        <h4>Convocatoria extraordinaria</h4>
        <% if (examenes) { %>
        <table class="table table-striped">
            <thead class="thead-light">
                <tr>
                    <th scope="col">Fecha</th>
                    <th scope="col">Hora</th>
                    <th scope="col">Asignatura</th>
                    <th scope="col">Duración</th>
                </tr>
            </thead>
            <tbody>
                <% examenes.extraordinaria.forEach((exam)=>{ %>
                <tr>
                    <td><%= exam.fecha ? exam.fecha : '-' %></td>
                    <td><%= exam.hora ? exam.hora : '-' %></td>
                    <td><%= exam.asignatura %></td>
                    <td><%= exam.duracion ? exam.duracion : '-' %></td>
                </tr>
                <% }); %>
            </tbody>
        </table>
        <% } %>
        <a class="btn btn-secondary" href="<%= contextPath %>/horarios_guardados" role="button">Atrás</a>
    </div>

</div>